// Mengenerate angka random menggunakan Math.random()
// Note : angka yang di generate selalu 0, dan tidak pernah 1
// Contoh :
function random() {
    return Math.random();
};

let angkaRandom = random();
console.log(angkaRandom);

// angka yang di generate selalu berbeda